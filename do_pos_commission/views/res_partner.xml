<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--    To add affiliated field in partner form-->
    <record id="view_res_partner_inherit_form" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit.sales.commission.users</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
           <!--  <field name="vat" position="after">
                <field name="is_commission"/>
            </field> -->
            <field name="barcode" position="after">
                <field name="hide_in_pos"/>
            </field>
        </field>
    </record>
</odoo>
